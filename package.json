{
  "name": "rails_sluggable",
  "version": "1.0.2",
  "description": "A Rails plugin for generating configurable model slugs.",
  "scripts": {
    "dev": "cd test/dummy && rails server",
    "clean": "rm -rf *.gem",
    "build": "gem build *.gemspec",
    "pubpush": "gem push *.gem",
    "release": "release-it"
  },
  "author": "afeiship",
  "license": "ISC",
  "keywords": [
    "rails",
    "tag",
    "slugify"
  ]
}
